<%= form_with(model: main_ingredient, local: true, class: 'round-borders') do |form| %>
  <% if main_ingredient.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(main_ingredient.errors.count, "error") %> prohibited this main_ingredient from being saved:</h2>

      <ul>
      <% main_ingredient.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :count %>
    <%= form.number_field :count %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
